// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;
contract truefalse{
    function check (int num) public pure returns (bool){
        if (num%4 ==0 ||  num%8 ==0 && num > 10)
        return true; 
        return false;
    }
}
